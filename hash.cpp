#include <cstdlib>
#include <iostream>
#include <cstring>

#include "hash.h"

using namespace std;

int hashmap::Hash(char* key){
  int hash = 0;
  int index;
  
  index = strlen(key);

  return index;
  
}
